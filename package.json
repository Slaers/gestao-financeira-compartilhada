{
  "dependencies": {
    "bootstrap": "^5.3.6",
    "chart.js": "latest",
    "firebase": "latest"
  },
  "devDependencies": {
    "typescript": "latest",
    "esbuild": "latest"
  },
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=public/bundle.js --platform=browser",
    "dev": "esbuild src/main.ts --bundle --outfile=public/bundle.js --servedir=public --platform=browser"
  }
}
